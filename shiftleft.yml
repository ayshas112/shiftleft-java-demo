build_rules:
  - id: allow-zero-findings
    finding_types:
      - vuln
      - secret
      
    cvss_31_severity_ratings:
        - critical
        - high

    threshold: 0
  - id: allow zero secrets
    finding_types:
        - secret
        
    threshold: 0    
